# Multistage Refinement Plan - 2026-07-05

## Goal

Improve the current `40960`-sample active-set Sobol estimator by using early samples to refine Stage 1 decisions and guide later estimation, without repeating local-overfit traps like Sobol seed selection.

Primary metric: public `adjusted_final_layer_score`.

Secondary local metrics:
- raw final-layer MSE
- budget utilization / score multiplier
- all-layers MSE
- per-class final-layer error by dead / kink / on

## Current Baseline

Current best public direction:
- `40960` effective samples: `20480` Sobol half-points plus antithetic pairs.
- Stage 1 diagonal analytical propagation classifies neurons as dead / kink / on.
- Stage 2 active-set Sobol MC samples non-dead neurons.
- Layer-30 fold skips confident on neurons.
- Dead-neuron analytical correction is added back.
- Submission `314954` added two-ended layer-30 refinement and improved public score slightly to `2.27e-7`.
- Submission `314957` added layer-29 borderline dead refinement on top of layer-30 borderline refinement and improved public score to `2.25e-7`.

Important negative results:
- `49152` samples used too much compute and was worse on leaderboard.
- Sobol scramble seed `314159` overfit local validation and regressed on leaderboard.
- Sphere/radial collapsed sampling helped at `16384` but lost at `40960`.
- Final-neuron top-k sample allocation was worse because it starved the base estimate.

## Working Hypothesis

Stage 1 is a strong cheap prior, not something to replace. Its main weakness is late-layer model bias from diagonal propagation, especially around classification thresholds.

Multistage sampling should therefore do two things:

1. Refine structural decisions:
   - demote weak `on -> kink`
   - promote weak `dead -> kink`
   - keep very confident dead/on decisions analytical

2. Use pilot information to improve later estimates without starving the full final-layer average:
   - avoid reducing the base sample count for all neurons
   - reuse pilot samples in the final estimate
   - prefer conservative corrections and stratification over arbitrary tilting

## Design Constraints

- Keep effective sample count near `40960` unless leaderboard data says otherwise.
- Keep budget use near the proven `45%` envelope.
- Do not choose QMC seeds by synthetic validation alone.
- Do not submit a change based only on one synthetic seed group.
- Prefer changes that preserve normal Sobol sampling unless proper weights or stratification are available.
- Record every submission result in `bench_logs/submission_learnings_YYYY-MM-DD.md` before starting the next variant.

## Experiment Track A - Sharpen Stage 1 Classification

### A1. Instrument two-ended layer-30 refinement

Question: how many neurons are actually changed by the 5% pilot, and which side contributes the gain?

Collect per MLP:
- number of `on -> kink` demotions at layer 30
- number of `dead -> kink` promotions at layer 30
- final-layer MSE delta vs `normal fold-on 3.00`
- FLOP delta

Decision rule:
- If most gains come from `dead -> kink`, focus next on cheaper dead probes.
- If most gains come from `on -> kink`, focus next on fold threshold and on-probe cost.
- If changes are rare but high impact, make the pilot targeted to borderline neurons only.

### A2. Target only borderline layer-30 neurons

Instead of probing all layer-30 dead/on neurons:
- probe on-neurons with analytical alpha in `[3.0, 4.0]`
- probe dead-neurons with analytical alpha in `[-4.0, -2.5]`

Expected benefit:
- lower pilot FLOPs
- similar correction quality

Validation:
- local seeds `0..4` and `5..9`
- compare adjusted score and raw final MSE against current two-ended all-probe variant

### A3. Try layer-window refinement

Extend conservative two-ended refinement to late layers only:
- layer 29
- layer 30
- optionally layer 31 only if no regression

Risk:
- more pilot FLOPs
- dynamic mask changes can alter downstream active sets and increase compute

Decision rule:
- Only keep if public-like validation shows score improvement after multiplier.

## Experiment Track B - Pilot-Guided Corrections

### B1. Late-layer residual diagnostics

Use pilot/full local runs to bin final-layer error by:
- final neuron class: dead / kink / on
- analytical alpha bin
- pilot alpha bin
- analytical variance
- layer-30 promoted/demoted status

Goal:
find systematic signed residuals that can be corrected cheaply.

Do not submit any correction until the bias is consistent across fresh seed groups.

### B2. Conservative residual correction

If diagnostics show stable bias, add a small correction only for the affected bin.

Candidate form:
- correction = `lambda * analytical_mu` for a specific class/bin
- or correction = `lambda * (pilot_mean - analytical_mean)` for promoted/demoted neurons

Guardrail:
- tune `lambda` on one seed group, validate on a disjoint group.
- treat leaderboard contradiction as decisive.

## Experiment Track C - Guided Next Batch Sampling

### C1. Stratified activation-pattern sampling

Use pilot samples to identify late-layer activation strata, then ensure the next batch covers underrepresented strata.

Safer than arbitrary tilting because strata can have explicit weights.

Open design questions:
- which low-dimensional stratum label to use?
- layer-30 active count bucket?
- number of near-boundary neurons?
- sign pattern of a small influential neuron subset?

### C2. Boundary-focused sampling with correction

Oversample near uncertain late ReLU boundaries only if we can attach weights or a controlled correction.

Risk:
- unweighted tilting can look good locally and fail on leaderboard.

Initial stance:
- do not implement until Track B diagnostics identify specific boundary-driven error.

## Immediate Next Step

Next candidate should refine earlier dead-neuron gates conservatively, starting from the leaderboard-confirmed layer-29 + layer-30 probe:
- at layer 29, probe dead-neurons with analytical alpha in `[-4.0, -3.0]` and promote to kink if pilot alpha `> -2.5`
- at layer 30, probe on-neurons with analytical alpha in `[3.0, 4.0]` and demote to kink if pilot alpha `<= 3.0`
- at layer 30, probe dead-neurons with analytical alpha in `[-4.0, -3.0]` and promote to kink if pilot alpha `> -2.5`

Rationale: local validation improved over layer-30-only borderline refinement on both seed groups while probing only about `40-45` total columns, and submission `314957` confirmed a small public improvement.

## Iteration Log

### 2026-07-05

- Created plan after submission `314954` improved to public score `2.27e-7`.
- Current best direction is two-ended Stage 1 refinement, not sample-count scaling or QMC seed tuning.
- Instrumented `eval_variants.py` with layer-30 demotion/promotion counts.
- All-probe two-ended refinement changed about `5` on-neurons and `0.8-1.0` dead-neurons on average, but probed about `158-164` columns.
- Borderline-only `4/-4` refinement changed nearly the same useful neurons while probing only about `26-29` columns, and was best or tied-best on both local seed groups.
- Isolated probes suggest the dead-side correction carries most of the raw-MSE gain; on-side still adds a small extra improvement when combined.
- Tested dead refinement further back than layer 30. Layer-29 + layer-30 borderline refinement was consistently better than layer-30-only on both seed groups (`5.530e-7` vs `5.550e-7`; `2.756e-7` vs `2.784e-7`).
- Wider dead-only windows were unstable: `l24-30` was best on seeds `5..9` but much worse on seeds `0..4`, so do not submit that without stronger evidence.
- Current next candidate is `l29+30 borderline 4/-4`; avoid layer-31 refinement for now because it repeatedly regressed.
- Submission `314957` confirmed `l29+30 borderline 4/-4` on the public leaderboard: adjusted score `2.25e-7`, final-layer MSE `4.94e-7`, budget used `45.82%`.
- Tested final-layer-only always-on threshold sweeps on top of `l29+30 borderline 4/-4` using full `40960` Sobol samples from the tracked artifact. Thresholds `3.25`, `3.50`, and `4.00` all regressed adjusted score: seeds `0,1` baseline `3.975e-7` vs best threshold `3.976e-7`; seeds `5..9` baseline `2.655e-7` vs best threshold `2.665e-7`. The final-on sampled path adds compute and did not reduce raw final MSE enough, so deprioritize hard final-layer on demotion.
- Tested antithetic paired pilot rows for the layer-29/30 borderline probes. It changed a few gate decisions but tied/slightly regressed at `40960`: seeds `0..4` baseline `5.098e-7` vs paired `5.099e-7`; seeds `5..9` baseline `2.655e-7` vs paired `2.656e-7`. Do not move paired pilot into `estimator.py` unless new evidence appears; the public-confirmed unpaired pilot remains the baseline.
- Tested layer-30 kept-on soft clipping correction for borderline on probes: add `lambda * mean(max(-pre_on_pilot, 0))` to kept layer-30 on means for `lambda` in `0.25`, `0.50`, `1.00`. At `40960`, `lambda=0.25` was a microscopic win on seeds `0..4` (`5.098474809e-7` vs baseline `5.098478072e-7`) but regressed on seeds `5..9` (`2.655407461e-7` vs `2.655277664e-7`), while larger lambdas also regressed/tied. Do not promote this correction without stronger diagnostics; on-neuron error is probably not simply missing rare negative-tail clipping at layer 30.
- Tested whether very large final always-on neurons are systematically underestimated in the current `l29+30 borderline 4/-4` champion. Across seeds `0..9`, final on neurons had seed-dependent bias rather than a uniform sign. By analytical final alpha, `alpha >= 6` had only mild negative bias (`-7.94e-5`, under fraction `0.50`); by predicted activation, the top `95-100%` on-neuron quantile was overestimated on average (`+2.75e-4`, under fraction `0.419`). Conclusion: the strong "large always-on underestimation" hypothesis is false; underestimation is seed/neuron-specific, not monotone in alpha or activation size.
- Tested targeted final always-on analytical blends for large neurons: masks `alpha >= 6` and top `10%` predicted final-on activation, blend weights `0.02`, `0.05`, `0.10`. At `40960`, tiny `0.02` blends improved seeds `0..4` (`alpha6`: `5.070e-7`, `pred90`: `5.086e-7`, baseline `5.098e-7`) but regressed badly on seeds `5..9` (`alpha6`: `4.395e-7`, `pred90`: `3.234e-7`, baseline `2.655e-7`); larger blends blew up. Do not use fixed positive analytical blending for large final-on neurons; any future correction needs an adaptive sign/proxy.
- Ran full public split (`1,000` MLPs) post-algorithm diagnostic for `l29+30 borderline 4/-4`: correlate per-MLP weight norm features against raw final-layer MSE. Result: no meaningful evidence that the algorithm does worse on larger-norm networks. Total Frobenius norm had Pearson `+0.031`, Spearman `+0.062`; mean layer Frobenius was the same. Final-layer Frobenius was near zero/negative (`-0.006` Pearson, `-0.016` Spearman). Quartile mean final MSE by total norm was non-monotone (`6.95e-7`, `6.24e-7`, `7.26e-7`, `7.01e-7`). The strongest observed relationship was with estimator compute/utilization, negative (`flops_used` Spearman `-0.284`), likely reflecting active-set structure rather than raw weight scale.
- Ran full public structural diagnostic excluding raw weight norm as the main hypothesis. Meaningful pre/post-prediction difficulty signals were activation scale and class geometry: `anal_final_var_mean` Spearman `+0.607` with final MSE; `final_on_pred_mean` `+0.581`; `pred_final_mean` `+0.570`; `pred_l30_mean` `+0.562`; `pred_final_std` `+0.561`; `pred_l30_std` `+0.555`; `final_on_alpha_ge6` `+0.347`; `final_on_frac`/`l31_on` `+0.331`. More final/l30 kink neurons correlated with lower error: `final_kink_frac`/`l31_kink` Spearman `-0.305`, `l30_kink` `-0.301`. Interpretation: algorithm 14 struggles more on high-activation, high-variance, always-on-heavy collapsed networks, not high-norm networks.
- Tested first dynamic allocation policy keyed by `anal_final_var_mean` public quartiles: easy bucket uses fewer samples, middle/hard stay at `40960`. On public mini, aggregate score looked promising (`easy30` `2.553e-7` vs baseline `2.678e-7`), but on public full and using the correct mean of per-MLP adjusted scores it slightly regressed: baseline `3.062229140e-7`, `easy20/mid40/hard40` `3.067427230e-7`, `easy30/mid40/hard40` `3.068627775e-7`. Aggregate `mean(final_mse)*multiplier(mean_flops)` misleadingly favored dynamic (`3.0295e-7` for easy30), so `eval_variants.py` was corrected to report the leaderboard-consistent per-MLP mean score. Conclusion: dynamic allocation remains plausible, but easy-bucket sample cuts alone are not enough; need either better gates or actual hard-bucket extra samples/targeted work.
- Created `examples/15_dynamic_allocation.py`: algorithm 14 plus analytical-final-variance dynamic allocation with `30720` easy, `40960` medium, `49152` hard samples. Local validation passed with `SOBOL_POINTS_PATH=/tmp/whest_sobol_points_24576.npz uv run whest validate --estimator examples/15_dynamic_allocation.py`; a one-MLP local run also completed. Mini diagnostic for `easy30/mid40/hard49` was promising against fixed `40960`: per-MLP adjusted score `2.502437227e-7` vs `2.633383014e-7`, with hard-bucket final MSE dropping from `1.132e-6` to `8.493e-7` and easy-bucket final MSE rising from `3.337e-7` to `3.844e-7`.
- Submission `315004` tested algorithm 15 on the leaderboard and regressed to public adjusted score `2.4639658682612954e-7` vs current best `2.25e-7`. Treat the mini improvement as local overfit or insufficient gate robustness. Do not resubmit this exact `anal_final_var_mean` quartile policy; dynamic allocation needs a better hardness classifier and full-split/per-MLP adjusted-score validation before another submission.
- Built `scripts/learn_allocation_policy.py` to frame allocation as supervised learning on public mini: features from analytical Stage 1 plus a `30720` base run, labels/costs from fixed `30720`, `40960`, and `49152` per-MLP adjusted scores. Mini oracle is strong (`2.00258e-7`) and action counts are balanced-ish (`30720`: 48, `40960`: 29, `49152`: 23), so allocation has headroom. However simple greedy trees did not beat the analytical-variance rule in 5-fold CV: best tested CV score was depth-1/min-leaf-16 `2.52726e-7`, versus fixed `30720` `2.52985e-7`, fixed `40960` `2.63338e-7`, and current anal-var quartile rule `2.50244e-7`. Interpretation: one can learn on mini, but the signal is weak/noisy for tiny trees; next policy should use stronger features, conservative easy gating, or train/validate on full before any submission.
- Added v2 allocation features from the `30720` base run (`mc_vars`/variance-of-mean proxies and analytical-vs-MC disagreement) plus ridge-delta contextual-bandit policy evaluation. On cached mini v2, new disagreement features showed weak signal (`stat_base_l30_anal_diff_mean` Spearman `+0.149` with oracle label), but learned policies still did not beat the hand analytical-variance rule: best ridge-delta CV was `2.53110e-7` (`alpha=10`, margin `0`, action counts `45/35/20`), best v2 tree CV was `2.57481e-7`, versus anal-var quartile `2.50244e-7` and oracle `2.00258e-7`. Conclusion: mini has allocation headroom, but learned mini policies are not robust enough yet; use full-split CV or stronger/less noisy marginal-value features before porting a classifier.
- Tested convergence-rate/prefix features by adding fixed prefix runs at `10240`, `20480`, and `30720` to the mini allocation cache. Features included final/l30 drift `10->20`, `20->30`, drift ratios, max drift, HHI, and top-10 drift share. Prefix features had weak label signal (`prefix_final_drift_10_20_mse` Spearman `-0.155`, `prefix_l30_drift_10_20_mse` `-0.128`, `prefix_final_drift_ratio` `+0.109`), but did not improve allocation policies: best ridge-delta CV worsened to `2.59042e-7` and best tree CV was `2.57322e-7`, still worse than anal-var quartile `2.50244e-7`. Conclusion: this naive prefix-convergence measurement is not enough; if using convergence, need a better target such as per-neuron variance reduction or direct predicted delta, likely trained on full.
- Case-studied mini MLPs where `49152` worsened vs `40960` despite more samples. In regressions like `heidi-willis` and `david-tyler`, class decisions were mostly unchanged and final always-on neurons dominated the loss increase: `heidi-willis` final-on MSE `1.487e-6 -> 2.635e-6`, `on_alpha>=6` `2.104e-6 -> 3.795e-6`; `david-tyler` final-on `2.238e-6 -> 3.030e-6`. The `49152` estimate moved in the same direction as the existing `40960` error (`corr(e40, move)` `+0.397` and `+0.332`), so extra Sobol samples reduced/changed sampling noise in a way that removed lucky cancellation and moved high-alpha on neurons farther from ground truth. In improvement cases (`cathy-butler`, `thomas-parsons`), the same decomposition had strong negative correlation (`-0.897`, `-0.831`), meaning the added samples moved estimates toward truth. Conclusion: more samples can worsen because the estimator is biased and QMC prefixes are not monotone; sample count should be allocated by predicted error-movement direction, not just variance/hardness.
- Tested learning a global adaptive always-on alpha threshold on public mini with actions `3.00`, `3.25`, `3.50`, `4.00` at fixed `40960`. All fixed thresholds were almost tied (`3.00`: `2.63338e-7`, `3.25`: `2.63021e-7`, `3.50`: `2.63233e-7`, `4.00`: `2.63780e-7`); oracle threshold choice only reached `2.62395e-7`, so global-threshold headroom is tiny. Best ridge cost model CV was `2.62908e-7`; trees were around `2.6333e-7`. Conclusion: a learned global alpha threshold is too blunt; if adapting alpha, learn neuron/layer-specific on-risk or hard-mode probe/demotion rules instead.
- Tested joint per-MLP mode selection over `(on_threshold, sample_count)` on public mini with modes: `(3.0,30720)`, `(3.0,40960)`, `(3.0,49152)`, `(3.25,30720)`, `(3.25,40960)`, `(3.5,30720)`, `(3.5,40960)`, `(4.0,30720)`, `(4.0,40960)`. Oracle score improved to `1.99606e-7`, slightly better than sample-only oracle `2.00258e-7`, so joint action space has real headroom. Fixed best mode was `(3.0,30720)` at `2.52985e-7`; a depth-1 tree CV reached `2.48535e-7`, beating the previous anal-var quartile mini rule `2.50244e-7`; ridge cost model was weaker (`2.54289e-7`). Learned depth-1 tree selected a mix of `(3.0,30720)`, `(3.0,49152)`, `(3.25,30720)`, `(3.5,30720)`, `(3.5,40960)`. This is promising mini-only evidence for joint alpha/sample per-MLP policies; needs full-split CV before any estimator port/submission.
- Ran the same joint `(on_threshold, sample_count)` mode menu on the full public split (`1,000` MLPs, `9,000` mode evaluations). Full baselines: algorithm-14-equivalent `(3.0,40960)` `3.062229140e-7`; best fixed mode was `(3.5,40960)` `3.042797496e-7`; oracle over modes `2.372046785e-7` with broad action support. Learned policies: best ridge cost model CV `3.033978384e-7` (`alpha=0.01`, diverse action counts), depth-1 tree CV `3.059449176e-7`, deeper trees worse. Interpretation: joint mode learning has real full-split signal but current learned gain is modest (`~0.9%` over algorithm 14, `~0.3%` over best fixed mode), so a direct port is risky unless a simple robust policy can be extracted and validated; however fixed `on_thresh=3.5` at `40960` deserves focused validation as a surprisingly strong baseline.
- Focus-validated fixed `on_thresh=3.5`, `40960` as an active estimator candidate with the same public mini CLI harness as algorithm 14. Validation/local/subprocess checks passed with `0` failures. Public mini was only a hair better than algorithm 14: `2.661796456e-7` vs `2.662116209e-7`; raw final MSE improved (`5.91965e-7` vs `5.93381e-7`) but compute utilization rose (`45.73%` vs `45.61%`). Conclusion: fixed `3.5` is not a strong mini signal; submitting would be a small, uncertain leaderboard probe, not a high-confidence improvement.
- Compared current single Sobol `40960` against `5` independent scrambled Sobol/RQMC folds of `4096` half-points each (`40960` antithetic samples total) on public mini. RQMC was not a drop-in replacement: adjusted score worsened `2.633383014e-7 -> 2.997672318e-7` (`+13.8%`), raw final MSE `5.933084134e-7 -> 6.755754607e-7` (`+13.9%`), all-layer MSE `1.150907265e-6 -> 1.293921798e-6`; FLOPs were essentially equal. However fold variance was a useful uncertainty proxy: fold variance mean Spearman with RQMC final MSE `+0.558`, with single-Sobol final MSE `+0.521`. Conclusion: do not replace the main Sobol net with independent RQMC folds, but RQMC/fold replicates may be useful as a decision/uncertainty pilot.
- Tested simpler continuous variance allocation on public mini: `N_i = clip(40960 * sqrt(anal_final_var_mean / 0.0218483), 16384, 81920)` using a temporary `40960`-half-point Sobol artifact. Result was a very small positive: adjusted score `2.633383014e-7 -> 2.631886336e-7`, raw final MSE `5.933084134e-7 -> 5.653238857e-7`, all-layer MSE worsened `1.150907265e-6 -> 1.211246382e-6`, mean compute fell `1.227569e11 -> 1.207303e11`; mean sample count `40566`, range `25604..67596`. This formula is saner than quartile buckets on mini but the gain is tiny and still mini-only; needs full/public-CV before any port.
- Retested continuous variance allocation with `V_ref=0.02143` on public mini. This improved the mini result: adjusted score `2.620027726e-7` vs fixed `40960` `2.633383014e-7`, raw final MSE `5.572568157e-7`, all-layer MSE `1.203657401e-6`, mean compute `1.219035e11`, mean samples `40960`, range `25852..68252`. This is the current best simple scalar sqrt-var setting on mini (`~0.5%` adjusted improvement), but still mini-only and unvalidated on full/leaderboard.
- Tested raising the sqrt-var anchor to `65536` with `V_ref=0.02143` and the same clip range `[16384,81920]`. On public mini, raw final MSE improved substantially (`5.933e-7 -> 3.995e-7`) and all-layer MSE improved (`1.151e-6 -> 7.868e-7`), but adjusted score regressed badly (`2.633e-7 -> 2.896e-7`) because mean compute/utilization jumped to `1.913e11` / `70.3%`; mean samples `64179`, with `13/100` MLPs at the `81920` cap. Conclusion: extra samples can buy accuracy, but the 65K anchor overspends under the score multiplier; any higher-anchor policy needs a more selective hard gate or lower cap.
- Tested analytical final activation momentum as allocation metric: `mean(mu_final^2)` and `mean(var_final + mu_final^2)` with median references, same sqrt formula and `[16384,81920]` clip. On public mini both improved raw final MSE versus fixed `40960` (`5.323e-7` and `5.314e-7` vs `5.933e-7`) but overspent enough to lose adjusted score (`2.684e-7`, `2.698e-7` vs fixed `2.633e-7` and avg-var rule `2.620e-7`); mean samples about `45k`, `5/100` at cap. Conclusion: momentum/second moment tracks scale and buys accuracy, but avg variance is a better compute-aware scalar for dynamic allocation.
- Fitted a convex surrogate per mini MLP from the measured Pareto table: `MSE(N) ~= b + a/N`, `flops(N) ~= d + cN`, then minimized the induced `alpha*N + beta/N` objective and rounded to nearest tested sample count. This in-sample oracle-style policy scored `2.11225e-7`, much better than fixed `40960` `2.63338e-7` and closer to discrete oracle `1.54543e-7`; it selected only `16384` for 56 MLPs and `40960` for 44 MLPs, with correlation `0.463` to the discrete oracle sample count. Caveat: this uses measured per-MLP MSE at all sample counts, so it is not deployable directly; useful as a smoother target for learning curve parameters/features.
- Implemented `scripts/learn_convex_allocation_policy.py` to learn from the convex-fit allocation targets using cached mini Pareto/features. Mini results were strong: ridge model predicting `log N*` reached CV score `2.26486e-7` (`alpha=1`, action counts `16384:40, 24576:18, 30720:20, 40960:14, 49152:7, 65536:1`), and direct cost tree CV reached `2.07107e-7` at depth 2, versus fixed `40960` `2.63338e-7`, best fixed `30720` `2.52985e-7`, convex-fit nearest `2.11225e-7`, and discrete oracle `1.54543e-7`. Major caveat: top ridge target features include fitted curve parameters (`curve_b`, `curve_log_a_over_b`, `curve_a`) that are not deployable unless estimated from base-run data; next step is to remove/replace non-deployable curve-fit features and rerun.
- Reran `learn_convex_allocation_policy.py` excluding non-deployable curve/compute-fit features (`curve_*`, `compute_*`). Performance collapsed: best deployable-feature ridge CV `2.56666e-7` (`alpha=1000`, mostly `24576` samples), direct cost trees `>=2.6905e-7`, versus fixed `30720` `2.52985e-7`, convex-fit nearest `2.11225e-7`, and discrete oracle `1.54543e-7`. Correlation with convex target was weak (`~0.24` max among available features). Conclusion: convex curve target is useful, but current online features do not estimate it well enough; need better base-run estimators of curve parameters (`a`, `b`) before this is deployable.
- Added optional `log1p_` feature augmentation to `learn_convex_allocation_policy.py` and reran no-leakage convex-target learning on mini. Best ridge improved slightly to `2.54617e-7` from `2.56666e-7`, but still lost to fixed `30720` (`2.52985e-7`) and remained far from convex-fit nearest (`2.11225e-7`). Tree results were unchanged. Conclusion: log transforms help ridge a little but do not solve the missing deployable curve-parameter estimation problem.
- Added optional deployable interaction features to `learn_convex_allocation_policy.py` (variance/drift to analytical-disagreement ratios, scale x on-fraction products, high-alpha on interactions). On mini with raw+log+interaction features, ridge did not improve: best CV `2.57056e-7` vs raw+log `2.54617e-7` and no-log `2.56666e-7`; trees also remained worse. Some interactions had signal (`prod_final_drift_on_frac` Spearman `+0.250`, `prod_l30_drift_on_frac` `+0.236`, `ratio_final_drift_to_anal_diff` `+0.215`), but current hand interactions do not make a deployable convex-N predictor.
- Ran conservative full-split shallow tree over cached joint modes `on3.0_n30720`, `on3.0_n40960`, `on3.5_n40960` with depth `1..2` and min leaf `50/100/150`. Baselines: fixed `on3.0_n30720` `3.08673e-7`, algorithm-14 equivalent `on3.0_n40960` `3.06223e-7`, fixed `on3.5_n40960` `3.04280e-7`; subset oracle `2.56412e-7`. Best CV tree was depth 2/min leaf 150 at `3.05229e-7`, selecting `on3.0_n30720` for 461 and `on3.5_n40960` for 539 MLPs. It beats algorithm 14 but loses to fixed `on3.5_n40960`, so this conservative tree is not worth porting. The proposed `65536` action was not included because the full joint cache does not contain it yet.
- Ran a full-cache hand-policy search over plausible combined features and modes. Best simple threshold found: if `stat_base_final_on_anal_diff_mean <= 0.0403593`, use `on3.5_n30720`, else `on3.5_n40960`. It scored `3.00078e-7`, beating algorithm-14 equivalent `3.06223e-7` and fixed `on3.5_n40960` `3.04280e-7`, but it was selected by full-cache search and has large worst-case regrets (e.g. `michael-morse` regret `+8.81e-7`, `ian-gonzalez` `+8.08e-7`). Treat as evidence that combined features can improve full-cache score, not as submission-safe without CV/holdout validation.
- Cross-validated a focused two-action full-cache policy: choose between `on3.5_n30720` and `on3.5_n40960` using pooled deployable features. Fixed scores: algorithm-14 equivalent `3.06223e-7`, fixed `on3.5_n40960` `3.04280e-7`, fixed `on3.5_n30720` `3.07382e-7`, two-action oracle `2.56091e-7`. A 1D threshold on `stat_base_final_on_anal_diff_mean` learned per fold scored `2.99342e-7` with thresholds around `0.0415` and action counts `672/328`, beating the non-CV hand threshold `3.00078e-7` and fixed `on3.5`. Ridge cost CV was weaker (`3.04442e-7` best), and trees were weaker (`3.04677e-7` best). This is the best simple full-CV rule so far, but still needs real estimator implementation/mini CLI validation before submission.
- Implemented the focused two-action rule naively in `estimator.py`: run `on3.5_n30720`, compute final-on analytical disagreement, and rerun `on3.5_n40960` if the feature exceeds `0.0415`. Validation/local/subprocess passed with no failures, but public mini failed badly: adjusted score `3.17234e-7`, raw final MSE `6.74353e-7`, compute utilization `49.92%`. This is worse than both algorithm 14 and fixed `on3.5`. Root cause: cache policy assumed choosing one mode, while implementation recomputed the high branch after already paying for the low branch; branch costs and possibly feature distribution no longer match. Do not submit this naive rerun implementation; a two-action policy must continue from the `30720` prefix state or be trained with rerun costs.
- Implemented `scripts/learn_stopping_bandit.py` as a one-step offline stop/continue diagnostic using cached mini Pareto/features. At `30720`, stopping score `2.52985e-7`, oracle stop/continue `1.71543e-7`, always-continue-to-best-future `1.95279e-7`; best ridge gain model CV `1.97042e-7` by continuing on `98/100` MLPs, but gain prediction correlation was only `+0.047`. At `40960`, stopping score `2.63338e-7`, oracle stop/continue `1.95279e-7`, best ridge CV `2.09635e-7` continuing on `99/100`, correlation `+0.154`. Conclusion: sequential framing has large mini headroom, but the learned one-step policies mostly exploit that “continue almost always” is good on this mini Pareto table; not yet a selective/deployable stopping rule.
- Submission `315071` tested the true-continuation version of the two-action strict-alpha gate and regressed to public adjusted score `2.5356820345891775e-7`. The gate saved compute (`38.44%` budget) but raw final-layer MSE rose to `6.75e-7`, so do not resubmit this exact stop rule.
- Submission `315074` tested continuous sqrt-variance allocation `N_i = clip(40960 * sqrt(V_i / 0.02143), 30720, 61440)` with a `(30720, 256)` shipped Sobol artifact and improved to public adjusted score `2.2452526444934443e-7`, final-layer MSE `4.76e-7`, all-layers MSE `1.10e-6`, budget used `46.30%`, mean effective compute `1.26e11`, and `0/50` public failures. Conclusion: smooth allocation around the proven `40960` anchor is now leaderboard-confirmed as a small win; quartile buckets and aggressive stop gates remain negative evidence.
- Submission `315122` confirmed staged all-layer classification refinement on top of the `315109` active estimator: public adjusted score `2.102e-7`, final-layer MSE `4.53e-7`, all-layers MSE `1.082e-6`, budget used `45.95%`, and `0/50` public failures. This makes staged all-layer classification part of Algorithm 15, while the old bucketed dynamic allocation remains rejected.
- Submission `315123` confirmed the cleaned-up Algorithm 15 sample envelope: `N_i = clip(49152 * sqrt(V_i / 0.02143), 30720, 61440)` with staged all-layer classification. Public adjusted score improved to `2.042047322113885e-7`, final-layer MSE `3.72e-7`, all-layers MSE `8.996e-7`, budget used `54.05%`, mean effective compute `1.47e11`, and `0/50` public failures. Conclusion: after staged classification, the old `45-46%` compute envelope was too conservative; smooth extra samples transfer, but fixed/max jumps still need score checks.
- Submission `315204` confirmed final-scored-row bookkeeping: keep final-row sample propagation unchanged, but return analytical filler for rows `0..30` and skip sampled intermediate row/dead-correction materialization. Public adjusted score improved slightly to `2.037596606578937e-7`, final-layer MSE stayed `3.72e-7`, all-layers MSE intentionally worsened to `8.164e-4`, budget used `53.93%`, and `0/50` public failures. Conclusion: all-layer diagnostics are now intentionally sacrificed; compare current variants by adjusted final-layer score and raw final-layer MSE only.
- Tested a conservative layer-29 confident-on fold into layer 30 on top of Algorithm 15. The implementation folded only layer-29 on neurons with analytical alpha at least `5.0` and propagated their linear contribution into layer 30/31 instead of materializing sampled activations. On the first 30 public-mini MLPs, disabled fold scored `2.425387e-7` with utilization `49.91%`; fold thresholds `6.0`, `5.0`, and `4.5` scored `2.464329e-7`, `2.457457e-7`, and `2.453136e-7` with utilization around `50.5-50.8%`. Raw final MSE was essentially unchanged. Conclusion: earlier on-folding is not free; the folded matrix products outweighed saved sampled columns. Keep the layer-30 fold, but do not extend it to layer 29 without a cheaper formulation.
- Tested a sharper W-gated layer-29 mean fold: fold only layer-29 on neurons whose outgoing squared-weight mass into layer-30 kink targets is small relative to their active-target mass. Calibration on public mini showed the kink-mass ratio is rarely small: min `0.264`, median `0.569`, only `0.07%` of layer-29 on columns had ratio `<=0.30` and alpha `>=5`. Strict gates (`ratio<=0.05/0.10`, alpha `>=5`) were no-ops. Looser gates fired but destroyed accuracy: on the first 30 public-mini MLPs, disabled fold scored `2.425380e-7`; `ratio<=0.30, alpha>=6` scored `2.783257e-7`; `ratio<=0.40, alpha>=6` scored `6.024870e-6`; `ratio<=0.50, alpha>=6` scored `1.083016e-3`, with almost no compute savings. Conclusion: mean-folding layer-29 on paths into layer-30 kink targets loses essential sample-level ReLU distribution; W gating does not rescue this fold unless a much stronger safe region is found.
- Tried feature-modified sqrt allocation using deployable late-structure features: `hardness = V * modifier / norm`, where modifier increases with final-on fraction, final `alpha>=6` fraction, low final-kink fraction, and low layer-30-kink fraction. Cache-best simple variant used coefficient `2`, `V_ref=0.024`, cap `65536`, and norm `9.698167`; exact public mini local run passed validation and scored rounded `2.61e-7`, raw final MSE `5.64e-7`, all-layers MSE `1.23e-6`, compute utilization `44.82%`, `0/100` failures. A milder coefficient-1/ref-`0.02143`/cap-`73728` variant scored rounded `2.62e-7`, raw final MSE `5.41e-7`, compute utilization `46.59%`.
- Submission `315076` tested the coefficient-2 feature-modified allocation and regressed to public adjusted score `2.2756611195776188e-7`, final-layer MSE `5.21e-7`, all-layers MSE `1.205e-6`, budget used `43.44%`, mean effective compute `1.18e11`, and `0/50` failures. Conclusion: the hand-composed structural modifier saved compute but lost raw final-layer accuracy; restore `315074` plain sqrt-var as current best before further work.
- Tested per-MLP Sobol rotations in `eval_variants.py` on top of the `315074`-style dynamic sqrt rule. Rotation basis modes: `w0` SVD, `sens30` diagonal-gated effective input-to-layer-30 sensitivity, and `sens31` diagonal-gated effective input-to-layer-31 sensitivity. Low-sample gates at `32768` did not fire on synthetic dynamic samples. Blanket `sens31` was unstable: seeds `0..4` improved (`5.775e-7 -> 5.258e-7`) but seeds `5..9` regressed badly (`2.903e-7 -> 3.796e-7`). Gated `sens30` for `N <= 49152` improved both groups: seeds `0..4` `5.775e-7 -> 4.561e-7`, seeds `5..9` `2.903e-7 -> 2.644e-7`, with rotation applied on about `60%` of MLPs in both groups. `w0 low49` also improved both groups but less, and `sens31 low49` regressed. Conclusion: if rotation is revived, target layer-30 sensitivity and gate it to lower/medium sample counts; do not use blanket final-layer sensitivity or W0-only as the main candidate.
- Tested rotation on top of fixed algorithm-14 sample count (`40960`, no dynamic allocation). Blanket `sens30` won seeds `0..4` but lost seeds `5..9`; blanket `w0` and `sens31` were worse. A variance-gated fixed-sample `sens30` rule (`rotate iff analytical final variance <= 0.0308592`) improved both synthetic seed groups in `eval_variants.py`: seeds `0..4` `5.111e-7 -> 4.183e-7`, seeds `5..9` `2.653e-7 -> 2.380e-7`, rotating about `60%` of MLPs. However the exact estimator port of this fixed algorithm-14 gated rotation failed on public mini: adjusted score rounded `2.80e-7`, raw final MSE `6.20e-7`, all-layers MSE `1.18e-6`, compute utilization `45.98%`, `0/100` failures. Conclusion: synthetic eval rotation wins do not transfer reliably; do not submit fixed algorithm-14 rotation. Active `estimator.py` was restored to `315074` plain sqrt-var after this check.
- Added `scripts/empirical_tail_rotation.py` to test prefix-informed tail rotations without changing `estimator.py`: run the `30720` prefix normally, estimate an empirical input-to-layer-30 kink-activation cross-covariance from prefix samples, blend its input-space Gram with the analytical layer-30 sensitivity Gram, rotate only the continuation block, and combine with the unrotated prefix. Public mini results: on first `10` MLPs `blend0.25` looked best (`2.012e-7` vs baseline `2.581e-7`); on first `30`, pure empirical `blend1` was best (`2.518e-7` vs `2.722e-7`); on full `100`, pure empirical `blend1` remained best with a smaller gain (`2.578694844e-7` vs baseline `2.626398486e-7`), raw final MSE `5.435e-7` vs `5.562e-7`, all-layers MSE `1.208e-6` vs `1.195e-6`, FLOPs `1.2365e11` vs `1.2230e11`. Conclusion: empirical prefix-informed tail rotation is the first rotation idea with a public-mini win on the actual dynamic allocation path, but the gain is modest and comes with extra compute/all-layer regression; next step is a careful estimator port of pure empirical tail-only rotation if we want a submission probe.
- Ported pure empirical tail-only rotation to `estimator.py` by capturing prefix input-to-layer-30 kink activation cross-covariance inside `_run_block`, SVDing it, and rotating only the continuation block. The exact public mini run did not reproduce the offline script win: adjusted score rounded `2.71e-7`, raw final MSE `5.61e-7`, all-layers MSE `1.22e-6`, compute utilization `46.50%`, `0/100` failures. Do not submit this port. Active `estimator.py` was restored to `315074` plain sqrt-var and validation passed. The offline script mismatch needs debugging before empirical tail rotation is revisited.
- Debugged the empirical-tail offline-vs-port mismatch. Current `estimator.py` and `scripts/empirical_tail_rotation.py` baselines match exactly on the first `10` public-mini MLPs, so the mismatch was not baseline scoring. The culprit is rotation construction: the offline win uses `np.linalg.eigh(E E.T)` while the port used `np.linalg.svd(E)`. These span the same subspace, but eigenvector/SVD signs differ, and finite Sobol estimates are not invariant to per-coordinate sign flips. On full public mini: `eigh` pure empirical scored `2.578736751e-7`, `svd` `2.685142840e-7`, sign-canonicalized `eigh` `2.641387694e-7`, sign-canonicalized `svd` `2.742725705e-7`. If porting this again, use the exact `eigh` Gram path and treat sign convention as part of the algorithm.
- Re-ported the corrected `eigh(E E.T)` empirical tail rotation and performed the byte-for-byte parity check: active `estimator.predict` matched `scripts/empirical_tail_rotation.py` `blend1` exactly on all `100` public-mini MLPs (`maxdiff=0`). Script-style scoring for both was `2.578694844e-7`, raw final MSE `5.435362452e-7`, all-layers MSE `1.207953957e-6`. However the WHest public-mini runner still scored the same parity-checked estimator at rounded `2.70e-7`, raw final MSE `5.58e-7`, all-layers MSE `1.25e-6`, compute utilization `46.51%`, `0/100` failures. Conclusion: prediction parity is solved, but the offline script is still too optimistic versus the WHest runner/effective scoring path; empirical tail rotation is not submission-ready. Active `estimator.py` was restored again to the `315074` plain sqrt-var rule and validation passed.
- Tested leaderboard-inspired final-layer-only output plus lower Sobol sample envelopes. Returning analytical filler for layers `0..30` while keeping final-row MC changed all-layer MSE to about `7e-4` but saved little by itself. The `24576..40960` sqrt-var envelope improved a 3-MLP smoke run (`2.22e-7`, raw `6.62e-7`, compute `33.8%`) but regressed on public mini (`2.68e-7`, raw `7.71e-7`, compute `33.7%`). A floor-seeking `12288..24576` envelope was worse even on the smoke run (`3.70e-7`, raw `1.97e-6`, compute `18.2%`). Conclusion: final-only output and sample cuts alone are not enough; any low-compute top-10-style path needs a better final-row estimator, influence pruning, or bias/control-variate correction. Active `estimator.py` was restored to the `315074` plain sqrt-var rule and validation passed.
- Investigated `keenanpepper/arc-whestbench-higher-moments-2026`. Added `scripts/higher_moment_closure_probe.py` to fit Edgeworth-style ReLU mean corrections using true pre-activation skew/kurtosis from the public higher-moment files. Oracle held-out results were extremely strong: on train MLPs `0..7`, test `8..11`, final Gaussian ReLU MSE improved `9.402e-7 -> 1.163e-8`. This confirms that higher moments can almost solve the ReLU mean residual if the true pre moments are known. A deployable diagonal cumulant propagation probe failed (`7.39e-5` adjusted, raw `7.39e-4`, compute at the `0.1` floor), showing cross-neuron cumulants matter; it was removed from the numbered examples so Algorithm 15 remains the current endpoint. Also tried sample-estimated final pre skew/kurtosis inside `estimator.py`; full correction regressed a 3-MLP smoke run (`2.63e-7`, raw `5.80e-7`, compute `45.9%`) and 0.25 damping was worse (`6.38e-7`, raw `1.40e-6`). Conclusion: the promising path is learning/deploying a closure for cross-neuron higher moments, not naive diagonal cumulants or noisy final-sample skew. Active `estimator.py` was restored to `315074` plain sqrt-var and validation passed.
- Added `scripts/learn_cumulant_closure.py` to distinguish value regression from screening/gating. On held-out MLPs `8..11`, expanded ridge features weakly predicted omitted third-order contractions (`stored` MSE `2.761e-4`, learned `2.588e-4`, corr `+0.291`) but the predicted mask still had useful oracle-value upside: keeping top `20%` by predicted magnitude would score `1.594e-4` if those selected omitted terms were computed exactly. Fourth-order screening was much stronger: `stored` MSE `1.974e-4`, learned `6.776e-5`, corr `+0.819`; keeping only top `10%` by predicted magnitude and using oracle selected values gave `5.070e-5`, and top `20%` gave `3.501e-5`. Conclusion: do not judge the idea solely by exact value regression; learned screening for which cumulant contractions to compute is especially promising for fourth order and still nontrivial for third order.
- Added downstream ReLU-mean gate scripts `scripts/learn_k3_downstream_gate.py` and `scripts/learn_k4_downstream_gate.py`. On held-out MLPs `8..11`, final transition isolation showed K3 is the bottleneck: Gaussian final MSE `9.402e-7`, stored-K3-only `4.552e-6` (harmful), true-K3-only `2.003e-7`, stored-K3+stored-K4 `1.341e-5`, true-K3+true-K4 `1.785e-8`. K4 gating is therefore only useful after K3 is fixed. Exact top-k K3 oracle masks have large upside (`keep 10% -> 3.256e-7`, `20% -> 2.364e-7`, `40% -> 2.014e-7`), but current learned final-transition masks are weak (`keep 40% -> 6.890e-7` for learned_abs_delta; `6.336e-7` for learned_abs_signed). Conclusion: the next gating problem should target final-transition K3 importance with better features/models; K4 screening alone can polish but cannot rescue bad/zero K3.
- Added `scripts/k3_block_pattern_attribution.py` and `scripts/sample_k3_projection_probe.py`. Block attribution confirmed dead-involving K3 pieces are zero, but naive stored on-heavy pair blocks are harmful: on the final transition, `zero` MSE `1.107e-6`, `stored_all` `5.241e-6`, `on_only` `1.199e-5`, `kink_involving` `7.657e-6`, while `full_oracle` is `2.011e-7`; only `KKK_diag` was slightly helpful. Scalar projection probing showed selected/full final K3 can be estimated well from Sobol final pre samples if true pre mean/variance are known: with oracle masks and true pre mean/var, sample K3 at `8192` already matched true top-20 K3 (`2.415e-7` vs `2.410e-7`) and full K3 (`2.080e-7` vs `2.003e-7`); raw sample ReLU mean was much worse (`2.108e-6`). However using sample-estimated pre mean/variance spoiled Edgeworth (`32768` sample K3 with sample moments `7.090e-7`), and diagonal analytical pre moments were unusable (`~9.58e-4`). Conclusion: K3 projection estimation is not the hard part; accurate pre mean/variance or a control-variate formulation around the current active-set mean is the hard part.
- Added `scripts/current_k3_control_variate_probe.py` to test sampled scalar K3 as a damped correction around the current `315074` active-set final prediction. On train MLPs `0..7`, test `8..11`, oracle K3 masks and oracle pre mean/variance produced only tiny transferable gains: at `8192` samples, keep `5%` fit `lambda=+0.035`, train `5.598e-7 -> 5.587e-7`, test `4.695e-7 -> 4.687e-7`; at `32768`, keep `5%` fit `lambda=+0.037`, test `4.695e-7 -> 4.686e-7`, and even test-oracle lambda only reached `4.686e-7` (or `4.679e-7` for keep `10%`). Conclusion: the K3 control-variate direction is real but too small on top of the current active-set estimator; current sampling/folding already captures most of this final K3 signal, so standalone K3 correction is not a near-term leaderboard jump.
- Added `scripts/learn_final_residual_closure.py` to test a top-submission-style final-only learned residual closure from cheap deployable features. Diagonal Gaussian baseline was unusable (`~9.6e-4` held-out final MSE; learned ridge only `~7.7e-4`). Full covariance/gain propagation was much better but still far from leaderboard scale: on MLPs `0..7` train, `8..11` test, covariance baseline `6.758e-5`, best ridge residual around `4.354e-5`. Conclusion: a simple ridge on diagonal/covariance features does not explain ranks `4..10` (`~1.6e-6` final MSE) or ranks `1..3`; top submissions likely use a more specialized final solver, stronger learned model, or additional public-data-derived structure beyond these cheap features.
- Tested the two-stage final-on hypothesis directly. Current estimator final error is concentrated in final-on neurons: on held-out higher-moment MLPs `8..11`, replacing final-on predictions with true final pre means improves total final MSE `4.695e-7 -> 1.190e-7`; replacing layer-30 on-source means alone improves `4.695e-7 -> 2.079e-7`, and replacing layer-30 kink+on source means improves to `1.191e-7`. Direct Sobol replacement of final-on pre means was too noisy (`32768` samples `5.060e-7`, not better than baseline), and a backward on-folded projection estimator through confident-on blocks behaved almost identically to direct sampling (`8192`: direct `1.410e-6`, folded `1.401e-6`; `16384`: direct `8.168e-7`, folded `8.189e-7`). Conclusion: the final-on/layer-30-on target is right, but naive sample or simple on-fold projection is not enough; the missing top-submission trick must reduce variance or solve the layer-30 projection more analytically.
- Revisited rotation specifically for final-on errors. A basis built from input-to-layer30-on-to-final-on sensitivity helped direct final-on pre-mean replacement at higher samples: on MLPs `8..11`, `32768` direct unrotated replacement `5.060e-7`, targeted `sens30_on_to_finalon` rotation `4.362e-7` (better than the `4.695e-7` current baseline in this offline replacement test); at `16384`, `sens31_finalon` was best (`6.601e-7` vs unrotated `8.168e-7`) but still worse than baseline. Generic `w0` and `sens30_all` were worse. SVD diagnostics showed the sensitivity map itself is low rank (rank ~6-14 for 80-95% energy), but the current final-on error is not aligned with the top sensitivity axes. A truncated subspace-only sampler was invalid because zeroing orthogonal input dimensions changed the distribution and produced huge errors. Conclusion: final-on-targeted full rotations have a small positive signal; low-dimensional subspace sampling would need orthogonal noise/control variates, not zeroed dimensions.
- Ported a conservative final-on targeted rotation continuation into `estimator.py` (`8192` extra rotated samples, blend `0.25`, gate final-on count `>=50`) and smoke-tested on `--seed 42 --n-mlps 3`. It validated but regressed badly: adjusted score `3.32e-7`, raw final MSE `5.57e-7`, compute utilization `58.8%` versus the restored baseline smoke point around `2.51e-7` / `44.9%`. The extra full-network rotated forward pass and `eigh` basis construction added too much tracked compute. Active `estimator.py` was restored to the `315074` plain sqrt-var rule and validation passed. Conclusion: the rotation signal is not deployable as a naive extra full forward continuation; any usable version needs cheaper basis construction, reuse of existing samples/state, or a low-rank/orthogonal-noise control variate that does not pay another full forward pass.
- Tried an in-pass direct final-on projection blend inside the existing layer-31 fold (`lambda=0.25`) to reuse current sampled state rather than running an extra forward pass. It validated but regressed the `--seed 42 --n-mlps 3` smoke run: adjusted score `2.59e-7`, raw final MSE `5.65e-7`, compute utilization `46.4%`. This is worse than the restored baseline smoke point (`~2.51e-7`, raw `5.65e-7`, compute `44.9%`) because it adds projection work without improving raw error. Active `estimator.py` was restored to the `315074` plain sqrt-var rule and validation passed. Conclusion: direct sampled final-on projection from the existing state is not enough; the final-on solver needs variance reduction or a better analytical projection, not a simple blend.
- Tested offline top-k final-on corrections using targeted rotation and selected final-on columns. Oracle selection confirmed concentration: at `32768`, top-8 oracle rotated blend reached `3.608e-7` vs current `4.695e-7`. Deployable proxies were weaker but still promising: `anal_diff` top-16 with rotation/blend `0.5` reached `4.074e-7`, and `col_norm` top-16 reached `4.322e-7`; at `8192`, `anal_diff` top-16 rotated blend `0.25` reached `4.331e-7`. Conclusion: selecting a small risky final-on subset is the right direction; the remaining issue is implementing the correction without paying a second full forward pass/basis cost.
- Added `scripts/two_stage_final_on_rotation.py` to test cheap broad sampling plus targeted final-on rotated repair. On held-out MLPs `8..11`, `N_base=8192` baseline final MSE `2.106e-6` at flops util `9.1%`; oracle top-16 rotated repair with `N_rot=32768` reached `1.363e-6`, while deployable proxies only improved to about `1.70e-6..1.77e-6` for top-32. With `N_base=16384`, baseline `1.145e-6` at util `18.3%`; best oracle top-16 rotated repair with `N_rot=32768` reached `8.187e-7`, while deployable proxy best was `col_norm` top-32 rotated blend `0.5` at `9.022e-7` and `anal_diff` top-32 rotated `9.620e-7`. Conclusion: the two-stage shape can hit the cheap top-10 raw-MSE regime, but proxy selection is still too weak to beat the current `315074` raw MSE; it may be useful only if implemented much cheaper than current sampling and scored by compute discount.
- Compared final-on gates in the two-stage setting (`N_base=16384`, `N_rot=32768`). High-alpha selection was better than earlier proxies: top-32 high-alpha rotated blend `0.5` scored `8.565e-7`, versus `col_norm` top-32 `9.022e-7`, `anal_diff` top-32 `9.620e-7`, and `alpha_col_norm` top-32 `8.983e-7`; oracle top-32 rotated replacement was `6.898e-7`. Low-alpha selection was worse (`9.842e-7` top-32 rotated), so the useful gate is the most confidently-on final neurons, not borderline-on neurons. Conclusion: use final alpha as the first deployable top-k gate for final-on targeted repair; combine with other features only after a learned ranker beats high-alpha.
- Added `scripts/low_rank_conditional_final_on.py` to test sampling only low-rank final-on sensitivity coordinates while integrating orthogonal input noise with diagonal Gaussian moment propagation. This naive conditional closure failed badly even with oracle selection: `N_base=8192` baseline `2.106e-6`, best conditional result around `9.31e-6`; `N_base=16384` baseline `1.145e-6`, best conditional around `9.65e-6`. Conclusion: the low-rank axis idea cannot ignore orthogonal covariance/nonlinear effects via a diagonal conditional approximation; any low-rank conditional model needs a much better orthogonal-noise closure or a control-variate formulation around actual samples.
- Temporarily forced `estimator.py` to a fixed `8192`-sample base and ran real public mini. It hit the compute floor (`mean_score_multiplier=0.10027`, utilization `9.52%`) but raw final MSE was too high (`3.36e-6`), giving adjusted score `3.37e-7`; all-layer MSE was `6.29e-6`. Active `estimator.py` was restored to the `315074` sqrt-var envelope and validation passed. Conclusion: cheap broad active-set sampling alone is not enough; it needs a much stronger final-on/top-k repair to compete with ranks `4..10`.
- Tested final-on-targeted rotation as the primary sample basis rather than an extra pass. Offline on MLPs `8..11`, `sens30_on_to_finalon` improved low-sample raw final MSE at larger N (`16384`: `1.145e-6 -> 9.624e-7`; `24576`: `8.779e-7 -> 5.892e-7` at util `27.4%`). A temporary estimator port with fixed `24576` primary rotation validated but regressed the `--seed 42 --n-mlps 3` smoke run: adjusted `2.99e-7`, raw final MSE `1.03e-6`, utilization `29.3%`. Active `estimator.py` was restored to the `315074` sqrt-var envelope and validation passed. Conclusion: primary rotation has offline promise but did not generalize in the smoke harness; do not promote without public-like confirmation or a better gate.
- Ran the fair public-mini comparison for fixed `24576` no-rotation versus fixed `24576` primary `sens30_on_to_finalon` rotation. No-rotation public mini: adjusted `2.65e-7`, raw final MSE `9.77e-7`, all-layer MSE `1.96e-6`, compute utilization `27.58%`. Primary rotation public mini: adjusted `2.84e-7`, raw final MSE `9.87e-7`, all-layer MSE `1.95e-6`, compute utilization `29.19%`, backend time much higher (`221.8s` aggregate). Rotation did not improve raw MSE and worsened adjusted score. Active `estimator.py` was restored to the `315074` sqrt-var envelope and validation passed. Conclusion: the final-on primary rotation does not transfer to public mini; drop this implementation path.
- External `mlp_cumulant_propagation` k=3 is not competitive as a replacement on public-mini examples (`k3` final MSE about `5e-6..1.4e-5` vs current active-set `~3e-7..1.6e-6`), but its direction is useful as a small teacher/prior. On mini indices `0..11`, shrinkage `active + lambda*(k3-active)` with lambda fit on `0..7` improved held-out final MSE at `30720` from `5.025e-7` to `4.247e-7`; sparse masks showed final-on neurons capture most of the useful direction (`final_on` test `4.409e-7`). Cheap feature distillation of the k3 direction did not beat baseline, so full k3 is a teacher signal, not directly deployable.
- Tested sample-based final-on corrections around active-set baseline. Extra full-network final-on sample correction improved held-out raw MSE but likely overspends if added on top. A cheap in-pass final-on sample correction using existing layer-30/final-fold state slightly improved raw MSE but lost adjusted score due to extra projection FLOPs. A nearly-free adaptive signed final-on scale correction was more promising: sign by mean analytical-minus-predicted final-on disagreement, scale `1e-4 * final_pred` on final-on neurons. Public mini subprocess improved current estimator from adjusted `2.57e-7`, raw `5.56e-7` to adjusted `2.52e-7`, raw `5.47e-7`, all-layer `1.20e-6`, utilization `44.12%`, `0/100` failures. Candidate for leaderboard submission after packaging check.
