\section{Discussion}
\label{sec:discussion}

\begin{figure}[hbt!]
	\centering
	\includegraphics[width=\linewidth]{figures/activation_function_shapes.png}
	\caption{The three activation functions used in the ablation study.}
	\label{fig:activation-function-shapes}
\end{figure}

The estimator is not a pure cumulant-propagation or covariance-propagation method \citep{wright2024analytic, wu2026estimating}. Instead, it combines analytical approximations with antithetic Sobol sampling. Its primary mechanistic contribution is using structural information about the MLP to sample more strategically: the diagonal Gaussian pass first predicts which neurons are dead, kink, and on; the pilot samples refine uncertain classifications; and the resulting activation structure determines both which neurons are sampled and how the sampled matrix products are evaluated. We hope our methods can serve as a sampling baseline for more sophisticated white-box estimators that exploit the same mechanistic structure.

A useful stress test is to ask what the estimator's premises depend on. We run two ablation studies, varying the nonlinearity and the architecture independently, to see whether the estimator's assumptions survive. Each ablation is run on a matched random MLP with width 256, depth 32, and He-initialized Gaussian weights.

Both ablations report the \emph{effective rank} of the activation covariance, which we define here. Let $\Sigma_\ell \in \mathbb{R}^{n \times n}$ be the empirical covariance of the layer-$\ell$ activations over the sampled inputs, with eigenvalues $\lambda_1 \ge \dots \ge \lambda_n \ge 0$. We use the participation-ratio form
\begin{equation}
	\erank(\Sigma_\ell)
	\;=\; \frac{\left(\sum_{i=1}^{n} \lambda_i\right)^{2}}{\sum_{i=1}^{n} \lambda_i^{2}}
	\;=\; \frac{(\tr \Sigma_\ell)^{2}}{\|\Sigma_\ell\|_F^{2}},
	\label{eq:effective-rank}
\end{equation}
which is scale-free, equals $k$ when $k$ eigenvalues are equal and the rest vanish, and drops toward $1$ as the spectrum concentrates on a single direction. It is a smooth stand-in for $\rank(\Sigma_\ell)$ that does not require a cutoff on near-zero eigenvalues --- which matters here, because the SiLU stack decays to an activation scale of $4.6\times10^{-5}$ and any absolute threshold would report its rank as a property of the threshold. Note that this is the participation ratio rather than the entropy-based effective rank of \citet{roy2007effectiverank}.

\subsection{Activation Functions}

\begin{figure}[hbt!]
	\centering
	\includegraphics[width=\linewidth]{figures/activation_function_neuron_maps.png}
	\caption{The activation magnitude $\overline{|a|}$ on a shared log scale for three networks with different activation functions.}
	\label{fig:activation-function-regimes}
\end{figure}

The first ablation varies the \emph{nonlinearity}. \Cref{fig:activation-function-regimes,fig:activation-function-covariance} repeat the diagnostic with ReLU, GELU \citep{hendrycks2016gelu}, and SiLU \citep{elfwing2018silu}. The latter two activations are smooth, see \cref{fig:activation-function-shapes}, and therefore do not produce the hard dead and always-on populations that the estimator exploits. 

\begin{figure}[hbt!]
	\centering
	\includegraphics[width=\linewidth]{figures/activation_function_covariance_comparison.png}
	\caption{Activation covariance comparison for the matched activation functions. Columns are ordered by each neuron's mean absolute correlation with the rest of its layer, so weakly coupled neurons collect at the top left and the correlated core at the bottom right; the white-to-red scale and its colorbar are shared across all panels. All three collapse onto a late correlated core, but only ReLU does so with the exact dead/on identities the submitted estimator exploits.}
	\label{fig:activation-function-covariance}
\end{figure}

Thus, although ReLU creates clean always-off and always-on regions, GELU and SiLU produce neither. Instead, under the initialization studied here, their activations gradually shrink with depth, so that by layer 32, the typical activation magnitude is close to zero even though neurons remain responsive to their inputs. This distinction is important when designing diagnostics. A method that declares neurons ``dead'' whenever their activation falls below a fixed absolute threshold could incorrectly label an entire deep SiLU layer as dead simply because the overall activation scale has decayed, which is a property of the threshold and not of the network; see \cref{fig:activation-function-regimes}.

Similar to ReLU networks, the correlation matrices of the activations in GELU and SiLU networks collapse onto a very correlated core, but the ReLU network is the only one that does so with the exact dead/on identities that the estimator exploits. The smooth activations do not produce the same hard regimes, and therefore the estimator's assumptions do not hold for them. We think a mechanistic method exploiting the low rank of the activations could be an interesting direction, but we haven't found a method that achieves higher accuracy than sampling.

\begin{table}[hbt!]
	\centering
	\small
	\caption{The six wirings, written out. For one input, $x \in \R^{d}$ is the layer input (a row vector, as in the rest of the paper), $W$ its weight matrix, $z = xW \in \R^{d}$ the pre-activation, $d = 256$ the width, and $\varepsilon = 10^{-5}$. No normalizer carries a learned affine ($\gamma = 1$, $\beta = 0$), so each row differs from \emph{plain} only by its normalizer. $\mu_i$ and $\sigma_i$ are the mean and standard deviation of neuron $i$'s pre-activation \emph{over the input population}, estimated once and then held fixed, which is what makes inference-time BatchNorm a per-neuron affine; $\bar z = \frac1d\sum_j z_j$ and $s_z$ are the mean and standard deviation of the $d$ coordinates of $z$ \emph{within a single input}. The last column is what the estimator actually depends on: whether the normalizer can change which side of the kink a pre-activation falls on.}
	\label{tab:architecture-definitions}
	\begin{tabular}{llll}
		\toprule
		Variant & Layer update & Normalizer $N(z)$ & Effect on $\operatorname{sign}(z)$ \\
		\midrule
		plain             & $x \mapsto \relu(xW)$                       & ---                                                              & --- \\
		\hline\\
		residual \citep{he2016identity} & $x \mapsto x + \relu(xW)$                   & ---                                                              & --- \\
		\hline\\
		BatchNorm \citep{ioffe2015batch} & $x \mapsto \relu(N(xW))$                    & $\dfrac{z_i - \mu_i}{\sigma_i + \varepsilon}$                      & per-neuron shift \\
		\hline\\
		LayerNorm \citep{ba2016layer} & $x \mapsto \relu(N(xW))$                    & $\dfrac{z - \bar z \mathbf{1}}{s_z + \varepsilon}$                 & per-input shift \\
		\hline\\
		RMSNorm \citep{zhang2019root}   & $x \mapsto \relu(N(xW))$                    & $\dfrac{z}{\sqrt{\frac1d \sum_j z_j^2} + \varepsilon}$             & none (positive scale) \\
		\hline\\
		pre-norm residual & $x \mapsto x + \relu(\mathrm{LN}(x)\,W)$    & LN, applied to the branch input                                    & per-input shift, upstream \\
		\bottomrule
	\end{tabular}
\end{table}

\subsection{Architectural Tweaks}
\label{sec:architecture-ablation}

The second ablation varies the \emph{architecture}. \Cref{fig:architecture-regimes,fig:architecture-covariance}, summarized in \cref{tab:architecture-ablation}, compare six wirings on the same weight tensors and ReLU activation function: plain $\relu(xW)$, a residual block $x + \relu(xW)$, batch, layer, and root mean square (RMS) normalization applied to the pre-activation, and a pre-norm residual block $x + \relu(\mathrm{LN}(x)\,W)$. BatchNorm is used in its inference-time form, a fixed per-neuron affine calibrated from population statistics. \Cref{tab:architecture-definitions} writes out all six.

\begin{figure}[hbt!]
	\centering
	\includegraphics[width=\linewidth]{figures/architecture_neuron_maps.png}
	\caption{The same two threshold-free views for the six architectures, directly comparable to \cref{fig:activation-function-regimes}. Grey in the right panel marks neurons that must be sampled; red and green mark the dead and always-on populations the estimator prunes and folds. BatchNorm and the pre-norm residual block are flat in \emph{both} panels --- uniform scale and uniform grey at every depth --- which is the visual form of an architecture that leaves nothing to classify. The unnormalized residual stack saturates the magnitude ramp, its median activation reaching $8.0\times10^{6}$ by depth 32.}
	\label{fig:architecture-regimes}
\end{figure}

As shown in \cref{fig:architecture-regimes}, the estimator's assumptions survive some of these architectural tweaks but not others. Residual reinforces the classification, because the skip connection preserves the sign of the pre-activation and therefore the dead and always-on populations. RMS preserves the classification, because it rescales the pre-activation by a positive scalar and therefore cannot move a neuron from one side of the kink to the other. LayerNorm preserves the classification in expectation, but shifts the pre-activation by a per-input amount that can move a neuron from one side of the kink to the other. On the other hand, BatchNorm and pre-norm residual destroy the classification, thus collapsing the estimator's assumptions.

\begin{table}[hbt!]
	\centering
	\caption{Architecture ablation on matched He-Gaussian weights (width 256, depth 32). Regime counts and effective rank are all measured at the scored final layer; the cold-prefix column is the mean over depth of the prefix width as a fraction of the layer width. BatchNorm and the pre-norm residual block leave nothing to classify.}
	\label{tab:architecture-ablation}
	\begin{tabular}{lrrrrrr}
		\toprule
		& \multicolumn{3}{c}{Final-layer regimes} & & & \\
		\cmidrule(lr){2-4}
		Architecture & dead & kink & on & Eff.\ rank & Cold prefix \\
		\midrule
		plain $\relu(xW)$          &  73 & 106 &  77 &  2.2 & 35.3\% \\
		residual                   & 117 &   5 & 134 &  1.0 & 42.4\% \\
		BatchNorm                  &   0 & 256 &   0 &  9.4 &  2.1\% \\
		LayerNorm                  &  60 & 130 &  66 & 11.4 & 34.6\% \\
		RMSNorm                    &  73 & 106 &  77 & 10.9 & 35.3\% \\
		pre-norm residual          &   0 & 256 &   0 & 94.8 &  2.0\% \\
		\bottomrule
	\end{tabular}
\end{table}

In our error analysis in \cref{sec:results}, we saw that the estimator's error is dominated by the on neurons, with RMSNorm, we expect the exact magnitude of the errors to shrink according to the rescaling. For residual connections, we expect the classification step in the estimator to be more accurate, and therefore the error to shrink as well. For layer normalization, it might take a more sophisticated estimator to identify the per-input shift and correct for it. For BatchNorm and pre-norm residual, the estimator's assumptions are violated, and the estimator reduces to plain sampling. These intuitions are confirmed in \cref{tab:architecture-ablation,fig:rank-collapse}, which show that the estimator's classification survive residual and RMSNorm, are partially violated by LayerNorm, and are completely destroyed by BatchNorm and pre-norm residual.

\begin{figure}[hbt!]
	\centering
	\includegraphics[width=\linewidth]{figures/architecture_covariance_comparison.png}
	\caption{Activation correlation matrices for the six architectures, the same view as \cref{fig:activation-function-covariance}, columns ordered by mean absolute correlation with the rest of the layer and annotated with effective rank. The residual stack concentrates into a single saturated correlated core by L16; BatchNorm develops a weaker late core; the pre-norm residual block stays near-diagonal to depth 32, which is the visual form of a representation that never collapses.}
	\label{fig:architecture-covariance}
\end{figure}

These different architectures also produce varying effective ranks of the activation covariance, which is a more quantitative measure of the collapse of the representation, as shown in different views in \cref{tab:architecture-ablation,fig:rank-collapse,fig:architecture-covariance}. As seen in \cref{fig:architecture-covariance}, the residual collapses the most to a near rank-one representation, which might be attributed to the sub-optimality in He-initialization for residual blocks \citep{yang2017mean}. BatchNorm and LayerNorm are in between, with BatchNorm collapsing more than LayerNorm. RMSNorm is an exception: it raises effective rank without changing a single regime label, since it rescales magnitudes but cannot move signs. Pre-norm residual block maintains a near-full-rank representation, which is consistent with its popularity in modern architectures \citep{radford2019language, xiong2020layer}.

Across layers, as shown in \cref{fig:rank-collapse}, we see the effective rank decreases the fastest for the residual stack, followed by the plain ReLU network. Interestingly, RMSNorm's effective rank decreases at the same rate as layer normalization, instead of trailing the plain ReLU network. Although batch normalization initially show a slower decay, it eventually matches the RMSNorm and layer normalization. \citet{daneshmand2020batch} prove that batch
normalization provably avoids rank collapse in randomly initialized deep networks, so we don't expect the effective rank to continue to decay in deeper networks. 

The pre-norm residual block maintains a near-full-rank representation across all layers, which is consistent with the visualizations in \cref{fig:architecture-covariance}, and might be the most difficult to develop a mechanistic estimator that attempts to exploit the low-rank structure of the representation.

\begin{figure}[hbt!]
	\centering
	\includegraphics[width=\linewidth]{figures/rank_collapse_comparison.png}
	\caption{Effective rank of the activation covariance versus depth for both ablations on shared axes. Changing the nonlinearity moves the scored-layer rank only between $1.4$ and $4.1$; changing the architecture moves it from $1.0$ to $94.8$. The plain and residual stacks collapse toward rank one, which is what creates the hard regimes; normalization slows the collapse and the pre-norm residual block halts it. RMSNorm is the exception to the correspondence: it raises effective rank without changing a single regime label, since it rescales magnitudes but cannot move signs.}
	\label{fig:rank-collapse}
\end{figure}

\clearpage
