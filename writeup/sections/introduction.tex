\section{Introduction}

The \whest{} challenge aims to estimate the mean post-ReLU activation of every neuron using only the network weights and a fixed compute budget \citep{arcCompetingSampling2025,arcWideRandomMlp2026}. With a large number of samples, direct Monte Carlo sampling can be highly accurate; however, it does not provide much insight into the structure of the network, and it is inefficient since it treats every neuron and every sampled path in the same way. A white-box estimator, by contrast, can use the weights to decide which parts of the network are structurally predictable. 

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/layer_collapse_neuron_classification.png}
    \caption{Monte Carlo diagnostic from the layer-collapse notebook. Neurons are classified by their preactivation range over 20k samples: always-off neurons never fire, always-on neurons pass linearly through ReLU, and near-kink neurons still require sampled nonlinear propagation. The upper panels show the layerwise regime counts; the lower panels show representative single-neuron preactivation marginals from layer 31, making the dead, kink, and on regimes visible at the distribution level.}
    \label{fig:layer-collapse-classification}
\end{figure}

In a ReLU network, the most useful structural distinction is how likely a neuron's pre-activation is to fall on either side of the ReLU threshold. Neurons that are negative for nearly all inputs (``dead'') contribute little, neurons that are positive for nearly all inputs (``on'') behave nearly linearly, and neurons whose pre-activations frequently fall on both sides of the threshold (``kink'') are, in our submission, where Monte Carlo sampling is most informative. We show \Cref{fig:layer-collapse-classification} as an example: at layer 1, all 256 neurons are in the kink regime; by layer 16, 29 neurons are dead and 29 are always-on; by layer 32, 73 neurons are dead (28\% of the layer), 77 are always-on, and 106 remain in the kink regime. More importantly, \Cref{fig:covariance-block-waterfall} reveals that the always-on neurons form a highly correlated dense core, concentrating most of the covariance structure into a small subset of neurons.

We note that the term ``dead'' is related to the dying-ReLU in the literature, where inactive ReLU neurons output zero across inputs and produce a vanishing-gradient failure mode \citep{lu2020dyingrelu}. Lu et al. also prove a stronger depth-asymptotic statement: under their initialization model, dying ReLUs are inevitable once the network is deep enough, with deep ReLU networks dying in probability as depth tends to infinity. 

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/relu_covariance_block_waterfall.png}
    \caption{Post-ReLU covariance matrices reordered by dead, near-identity, sparse, and dense blocks across depth in a matched random ReLU MLP. The plot is a rank-collapse diagnostic: the dense correlated core becomes visually distinct in later layers, while a larger sparse tier remains. It supports the same late-layer structural story as the layer-collapse figures, but the fold itself follows from the exact layer-30 always-on linearity condition.}
    \label{fig:covariance-block-waterfall}
\end{figure}

This submission is deliberately a hybrid estimator that combines analytical methods with strategic sampling. Its core is not to replace sampling everywhere, but to use a mechanistic approximation to decide where sampling should be trusted, where it should be avoided, and how sampled activations should be propagated. The estimator first propagates a diagonal Gaussian approximation through the network, using the results to classify neurons as dead, kink, or on. It then refines uncertain classifications with a small set of samples. Finally, it turns the resulting ReLU behavior into implementation decisions: dead neurons are omitted from the sampled propagation, and their reported means are supplied by the analytical pass. The remaining non-dead neurons are evaluated using Monte Carlo samples, with dense or sparse matrix multiplication chosen according to the sampled activation pattern. In the final two layers, the estimator applies an additional optimization: kink neurons remain sampled, while on neurons are treated as linear, allowing their contributions to be propagated analytically through the weights of the final two layers. This implementation thus captures the idea of ``competing with sampling'' \citep{arcCompetingSampling2025}: the code is faster because it follows the network's structure rather than treating every sampled coordinate uniformly as a black box.

\subsection{Contributions}

The main ingredients of the submitted estimator are as follows.

\begin{enumerate}[leftmargin=*]
    \item A diagonal moment-propagation pass that classifies neurons into dead, kink, and on regimes.
    \item A staged active-set refinement scheme that uses small pilot sample prefixes to correct high-impact classification errors, especially in late layers.
    \item Network-behavior-driven implementation decisions: neurons that are almost always active are handled analytically in the last two layers, mostly active groups use standard dense matrix multiplication, and truly sparse groups use a packed computation that skips zero activations.
    \item A compute-oriented implementation that keeps these structural operations inside \fnp{} and treats compute reductions as consequences of the same routing decisions.
\end{enumerate}

In essence, much of the final-layer mean can be explained by a small set of structural decisions: which neurons are dead, which are effectively linear, which columns are dense, and which sampled paths remain near a kink. The implementation choices described below are included when they follow from those structural decisions, rather than as a separate story about evaluator-specific accounting.